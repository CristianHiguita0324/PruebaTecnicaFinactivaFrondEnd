<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="">PruebaTecnicaFinactiva</a>
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
       
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="Municipios">
                <span class="oi oi-list" aria-hidden="true"></span> Municipios
            </NavLink>
        </li>
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="Regiones">
                <span class="oi oi-list" aria-hidden="true"></span> Regiones
            </NavLink>
        </li>
    </ul>
</div>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
